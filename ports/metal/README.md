# ports/metal

Thin forward to [`extmod/metalmod/port`](../../extmod/metalmod/port).

```bash
make -C ports/metal BOARD=X86_64_BIOS
make -C ports/metal BOARD=X86_64_BIOS run

make -C ports/metal BOARD=X86_64_UEFI
make -C ports/metal BOARD=X86_64_UEFI run
```
